# Removes auxiliary files generated during compilation, keeping the project directory clean.
$clean_ext = "synctex.gz nav snm thm soc loc glg acn vrb";

# Ensures that BibTeX is executed when compiling documents.
$bibtex_use = 2;
